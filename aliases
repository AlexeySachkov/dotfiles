
# Directories listing
alias l='ls -l --color --block-size=1024'
alias la='ls -la --color --block-size=1024'

# Changing directory
alias ..='cd ..'
alias ...='cd ../..'
alias cd..='cd ..'

# Grep
alias grep='grep --color'
alias grp='grep --color'

# Vim
alias v='vim'

# Git
alias gti='git'

# tar.gz
alias untargz='tar -zxvf'
