# vim: filetype=sh

# Directories listing
alias l='ls -lh --color'
alias la='ls -lah --color'

# Changing directory
alias ..='cd ..'
alias ...='cd ../..'

# Grep
alias g='grep -rn --color'
alias gi='grep -rni --color'

# tar.gz
alias untargz='tar -zxvf'
